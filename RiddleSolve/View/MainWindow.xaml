<Window x:Class="RiddleSolve.View.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:viewModel="clr-namespace:RiddleSolve.ViewModel"
        xmlns:view="clr-namespace:RiddleSolve.View"
        xmlns:converters="clr-namespace:RiddleSolve.Converters"
        mc:Ignorable="d" d:DataContext="{d:DesignInstance viewModel:MainWindowViewModel}"
        Title="MainWindow" Height="1000" Width="1000">
        <Window.Resources>
                <converters:BooleanMultiValueConverter x:Key="BooleanMultiValueConverter" />
        </Window.Resources>
        <Viewbox Stretch="Uniform">
                <Grid IsEnabled="{Binding HasSolution}">
                        <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <ItemsControl Grid.Column="0"
                                      Width="{Binding Width}"
                                      Height="{Binding Height}"
                                      ItemsSource="{Binding Images}"
                                      Opacity="{Binding HasSolution, Converter={converters:BooleanMultiValueConverter TrueValue=1, FalseValue=0.4}}">
                                <ItemsControl.ItemsPanel>
                                        <ItemsPanelTemplate>
                                                <Canvas />
                                        </ItemsPanelTemplate>
                                </ItemsControl.ItemsPanel>
                                <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                                <view:MovedImageControl Name="Image"
                                                                        Source="{Binding BitmapSource}"
                                                                        UseLayoutRounding="True" />
                                        </DataTemplate>
                                </ItemsControl.ItemTemplate>
                        </ItemsControl>
                        <ToggleButton Grid.Column="1"
                                      IsChecked="{Binding IsDisplayedSolved}">
                                <ToggleButton.Style>
                                        <Style TargetType="ToggleButton">
                                                <Setter Property="Content" Value="Show solved"/>
                                                <Style.Triggers>
                                                        <DataTrigger Binding="{Binding IsDisplayedSolved}" Value="True">
                                                                <Setter Property="Content" Value="Show unsolved"/>
                                                        </DataTrigger>
                                                </Style.Triggers>
                                        </Style>
                                </ToggleButton.Style>
                        </ToggleButton>
                        <TextBlock Grid.Column="0" Grid.ColumnSpan="2"
                                   Text="There is no solution"
                                   HorizontalAlignment="Center"
                                   VerticalAlignment="Center"
                                   FontSize="60"
                                   Visibility="{Binding HasSolution, Converter={converters:BooleanMultiValueConverter TrueValue=Collapsed, FalseValue=Visible}}"/>
                </Grid>
        </Viewbox>
</Window>
